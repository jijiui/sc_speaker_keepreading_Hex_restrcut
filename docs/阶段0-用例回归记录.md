# 阶段 0：用例回归记录

> 重新执行说明：激活 `C:\Studying\code\sc_speaker\.venv`（pwsh：`& ..\.venv\Scripts\Activate.ps1`），设置 `PYTHONPATH=src`，执行 `python scripts/manual_regression_stage0.py`（2025-11-09 本地环境）。

| 用例 | 描述 | 结果 | 备注 |
| --- | --- | --- | --- |
| UC-01 | 加载 CSV/TXT（编码嗅探 + 列映射） | ✅ 通过 | `uc01.csv` 被 repository 解析为 1 个流程、3 条事件 |
| UC-02 | 加载 Excel 多流程（多 Sheet） | ✅ 通过 | `uc02.xlsx` 成功解析出 `FlowA/FlowB` 两个 Sheet；说明 pandas/openpyxl 在本地可用 |
| UC-03 | 开始/暂停/重置计时 | ✅ 通过 | TimelineService 驱动 start/pause/reset，状态切换与计时正常 |
| UC-04 | OCR 自动对时 | ⚠️ 通过（伴警告） | 运行末尾仍有 `QThread: Destroyed while thread '' is still running`，记录为性能提醒 |
| UC-05 | 语音播报（TTSWorker） | ✅ 通过 | `_speak` → TTS 队列被后台线程消费，打印模拟播报日志 |
| UC-06 | 导出示例 CSV | ✅ 通过 | 写入 `%TEMP%/pvz_stage0_*/sample.csv` 并校验表头 |
| UC-07 | 迷你模式 / 置顶切换 | ✅ 通过 | 置顶开关与迷你工具条状态一致 |

> 2025-11-09 22:41:39 UTC+08:00：耗时约 5.88s；OCR/TTS 均可用，唯一异常为 TTS 线程销毁警告。

结论：src 架构下 UC-01~UC-07 全部运行通过。

---

## 2025-11-11 19:09:24 UTC+01:00（Stage 5 回归）

> 重新执行：`& ..\.venv\Scripts\Activate.ps1; $env:PYTHONPATH=src; python -m unittest discover tests; python scripts/manual_regression_stage0.py; python scripts/smoke_timeline.py --tick 2000 --lead 3000 --early`

| 用例 | 描述 | 结果 | 备注 |
| --- | --- | --- | --- |
| UC-01 | 加载 CSV/TXT（编码嗅探 + 列映射） | ✅ 通过 | `uc01.csv` 解析 1 个流程/3 条事件，与 Stage 0 基线一致 |
| UC-02 | 加载 Excel 多流程（多 Sheet） | ✅ 通过 | `uc02.xlsx` 正确解析 `FlowA/FlowB`，pandas/openpyxl 仍可用 |
| UC-03 | 开始/暂停/重置计时 | ✅ 通过 | TimelineService 控制 start/pause/reset，状态转换正常 |
| UC-04 | OCR 自动对时 | ⚠️ 通过（伴警告） | 收尾仍打印 `QThread: Destroyed while thread '' is still running`，性能表现与 11-09 相同 |
| UC-05 | 语音播报（TTSWorker） | ✅ 通过 | `_speak` 队列持续被消费，控制台输出播报内容 |
| UC-06 | 导出示例 CSV | ✅ 通过 | 生成 `%TEMP%/pvz_stage0_*/sample.csv` 并校验列头 |
| UC-07 | 迷你模式 / 置顶切换 | ✅ 通过 | 置顶复选框、迷你工具条按钮保持联动 |

> 结论：Stage 5 回归耗时约 7.5s，功能与 Stage 0 基线一致；唯一剩余警告为 Qt 线程销毁提示，暂记为已知问题，无新性能回退。
