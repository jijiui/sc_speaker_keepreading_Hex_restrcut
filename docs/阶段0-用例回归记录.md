# 阶段 0：用例回归记录

> 重新执行说明：先前的结果未在虚拟环境中运行，因此作废。已按 `agent.md` 要求激活 `C:\Studying\code\sc_speaker\.venv`（pwsh：`& ..\.venv\Scripts\Activate.ps1`），再执行 `python scripts/manual_regression_stage0.py`（2025-11-09 本地环境）。

| 用例 | 描述 | 结果 | 备注 |
| --- | --- | --- | --- |
| UC-01 | 加载 CSV/TXT（编码嗅探 + 列映射） | ✅ 通过 | `uc01.csv` 被 `_read_flows_from_file` 解析为 1 个流程、3 条事件。 |
| UC-02 | 加载 Excel 多流程（多 Sheet） | ✅ 通过 | `uc02.xlsx` 成功解析出 `FlowA/FlowB` 两个 Sheet；说明 pandas/openpyxl 在本地可用。 |
| UC-03 | 开始/暂停/重置计时 | ✅ 通过 | 通过 `_set_active_flow` + `start/pause/reset` 验证状态切换与 QTimer 启停逻辑。 |
| UC-04 | OCR 自动对时 | ✅ 通过 | 在虚拟环境中 `opencv-python==4.12.0.88 / numpy==2.0.2` 组合可正常启用 `OcrTimerAgent` 并接收 ROI。 |
| UC-05 | 语音播报（TTSWorker） | ✅ 通过 | `_speak("测试播报")` → TTS 队列被后台线程消费，打印模拟播报日志。 |
| UC-06 | 导出示例 CSV | ✅ 通过 | 通过 monkeypatch 保存对话框，将示例写入 `%TEMP%/pvz_stage0_*/sample.csv` 并校验表头。 |
| UC-07 | 迷你模式 / 置顶切换 | ✅ 通过 | `_apply_always_on_top` 与 `act_mini` 切换后窗口标志及 `_mini_mode` 都如预期变化。 |
