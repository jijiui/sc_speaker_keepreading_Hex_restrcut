# 重构任务日志

> 用于跟踪《重构任务单》执行进度，按完成顺序记录。时间采用本地时区。

| 时间 | 任务编号 | 任务原文 | 当前状态 | 完成情况 / 证明 |
| --- | --- | --- | --- | --- |
| 2025-11-09 | 0.1 | 创建 `feature/hex-prep` 分支，提交《前导说明》《重构计划》到 `docs/` | ✅ 已完成 | 已在 `feature/hex-prep` 分支上提交本次更新，`docs/` 下《前导说明》《重构计划》可用（证据：`git branch` 显示该分支，`git log` 含本提交）。 |
| 2025-11-09 | 0.2 | 生成/更新 `requirements.txt`，记录依赖版本（注明可选项） | ✅ 已完成 | （补跑）先激活 `C:\Studying\code\sc_speaker\.venv` 后执行 `pip list`/脚本，按虚拟环境的实际版本刷新 `requirements.txt`（PyQt6 6.10.0、opencv-python 4.12.0.88、pyttsx3 2.99 等），并注明激活要求。 |
| 2025-11-09 | 0.3 | 手动回归 7 个关键用例并记录结果 | ✅ 已完成 | （补跑）命令：`& ..\.venv\Scripts\Activate.ps1; python scripts/manual_regression_stage0.py`；结果 JSON + 《docs/阶段0-用例回归记录.md》全量通过，含虚拟环境说明。 |
| 2025-11-09 | 0.4 | （可选）编写 CLI 脚本模拟 `_check_announcements_simple` 以便后续测试 | ✅ 已完成 | （补跑）在虚拟环境下执行 `python scripts/smoke_timeline.py --tick 2000 --lead 3000 --early`，输出 16 次整点/提前播报，作为无 UI 冒烟脚本。 |
| 2025-11-09 19:38:00 UTC+08:00 | 1.1 | 新建 `core/domain.py`，迁移 `TimelineEvent` 与时间解析/格式化函数 | ✅ 已完成 | 拆出 `core.domain`，`pv_z_…py` 现通过 `from core.domain import TimelineEvent, parse_time_to_ms, format_ms_to_clock` 引用，消除了 UI 脚本对 dataclass/时间函数的直接定义。 |
| 2025-11-09 19:40:30 UTC+08:00 | 1.2 | 设计 `TimelineService` 管理 flows/计时推进/播报判定 | ✅ 已完成 | 新增 `core/services/timeline_service.py`（含 `AnnouncementDecision`、`advance/sync_elapsed` 等 API），`core/services/__init__.py` 导出服务供 UI 使用。 |
| 2025-11-09 19:42:30 UTC+08:00 | 1.3 | 引入测试框架并覆盖 `TimelineService` | ✅ 已完成 | 新建 `tests/test_timeline_service.py`（unittest），验证整点/提前/同步/next index 等行为；`python -m unittest tests.test_timeline_service` 通过。 |
| 2025-11-09 19:48:00 UTC+08:00 | 1.4 | 改造 `MainWindow`，接入 TimelineService 并保持 UI 行为 | ✅ 已完成 | `pv_z_…py` 现仅持有 `TimelineService`，移除 `self.flows/self.elapsed_ms` 等状态；`_on_tick`、OCR、全局提前/播报开关都委托服务。`scripts/manual_regression_stage0.py` 适配新接口，并在 `.venv` 下重跑 UC-01~UC-07 全绿。 |
| 2025-11-09 20:12:00 UTC+08:00 | Note | 统一提交阶段 0/1 余下改动（docs/阶段0 回归记录、requirements、CLI 冒烟脚本、agent 规范） | ✅ 已完成 | 按 `agent.md` 规范追加“英文注释”要求，补交 `docs/阶段0-用例回归记录.md`、`requirements.txt`、`scripts/smoke_timeline.py` 等文件，并确认所有记录均在 `.venv` 中复现。 |
| 2025-11-09 20:09:29 UTC+08:00 | 2.1 | 定义 `TimelineRepository` 接口并迁移 CSV/Excel 解析至 infra 层 | ✅ 已完成 | 新增 `core/ports/timeline_repository.py` 与 `infra/file_repository.py`，`MainWindow` 通过 `FileTimelineRepository` 加载流程，原有 `_read_*`、pandas/编码逻辑全部下沉；`scripts/manual_regression_stage0.py` 也改为调用仓储，保持 UC-01~UC-07 冒烟通过。 |
| 2025-11-09 20:13:12 UTC+08:00 | 2.2 | 封装 `PreferenceStore` 接口，抽离 `QSettings` 读写 | ✅ 已完成 | 新增 `core/ports/preference_store.py` + `infra/qsettings_store.py`，`MainWindow` 通过 `PreferenceStore` 读写 `ocr/roi`、`ocr/auto`、`opt/*`；`agent.md` 亦强调英文注释规范。 |
| 2025-11-09 20:18:59 UTC+08:00 | 2.3/2.4 | 设计 SpeechPort / TimeSourcePort，并让 TTS/OCR 适配器实现 | ✅ 已完成 | 新增 `core/ports/speech_port.py`、`core/ports/time_source_port.py`，`MainWindow` 支持注入自定义语音端口与时间源；默认适配 `TTSWorker` 与 `OcrTimerAgent`，并对 `scripts/manual_regression_stage0.py` 重新验证 UC-01~UC-07。 |
| 2025-11-09 20:21:11 UTC+08:00 | 2.5 | 提供简易 DI/工厂，MainWindow 通过构造注入 service+ports | ✅ 已完成 | `MainWindow` 构造函数现接收 TimelineService/Repository/PreferenceStore/SpeechPort/TimeSourcePort，`main()` 显式构建依赖后再传入，实现最小化 DI；冒烟脚本与 unit tests 已在 `.venv` 下复跑。 |
| 2025-11-09 20:46:04 UTC+08:00 | 3.1 | 拆分左侧控制区/右侧表格/迷你工具条为独立组件 | ✅ 已完成 | 新增 `ui/components.py`，`MainWindow._build_ui` 通过 `build_left_panel/build_right_panel/build_mini_toolbar` 构造控件，UI 结构更清晰，后续替换组件无需触及主窗逻辑。 |
| 2025-11-09 20:46:04 UTC+08:00 | 3.2 | 重写事件绑定，UI 仅调用服务/端口 | ✅ 已完成 | `MainWindow` 继续使用 `TimelineService`/各 Port，左侧控件在 builder 内部统一绑定；所有状态更新改由 `UiStateController` 驱动，UI 不再直接操作内部状态。 |
| 2025-11-09 20:46:04 UTC+08:00 | 3.3 | 引入统一状态管理器供 UI 订阅 | ✅ 已完成 | 新增 `ui/state.py`（`UiStateController`），集中处理状态文本与倒计时；`_update_clock` 等逻辑走同一入口，结合 `scripts/manual_regression_stage0.py` 冒烟确保 UI 变化无回归。 |
| 2025-11-09 21:52:53 UTC+08:00 | 4.0 | 建立 `src/` 架构并统一 `app.*` 导入（含 OCR 适配器、主程序、核心/适配层移动） | ✅ 已完成 | 创建 `src/app/` 包，`main.py`、`core/`、`infra/`、`ui/`、OCR 适配器全部迁入，`scripts/manual_regression_stage0.py`/`tests/test_timeline_service.py` 添加 `src` 路径注入；`python -m unittest tests.test_timeline_service`、`python scripts/manual_regression_stage0.py`、`python scripts/smoke_timeline.py --tick 2000 --lead 3000 --early` 均在 `.venv` 中通过。 |
| 2025-11-09 22:20:48 UTC+08:00 | 4.1–4.3 | 新增仓储单测、CI 工作流、统一运行脚本 | ✅ 已完成 | `tests/test_file_repository.py` 覆盖 CSV/Excel 解析；`.github/workflows/ci.yml`（Windows）安装依赖后运行单测与 smoke CLI；新增 `scripts/run_app.ps1` 与 `README.md`，当时文档化 `python -m app.main` 的运行方式（Stage C2 起统一更新为 `python -m app.bootstrap`）。 |
| 2025-11-11 12:48:29 UTC+01:00 | revert | 回滚到 266a466（保留 README/agent.md），恢复 UI 原始中文资源 | python -m unittest discover tests; python scripts/manual_regression_stage0.py; python scripts/smoke_timeline.py --tick 2000 --lead 3000 --early | - |
| 2025-11-11 13:05:00 UTC+01:00 | test | 新增编码守卫 & 自动 mock Qt 对话框的测试 | python -m unittest discover tests | - |
| 2025-11-11 13:20:00 UTC+01:00 | chore | Stage0 抑制 QFileDialog/QMessageBox，避免弹窗阻塞 | python -m unittest discover tests; python scripts/manual_regression_stage0.py; python scripts/smoke_timeline.py --tick 2000 --lead 3000 --early | - |
| 2025-11-11 12:48:29 UTC+01:00 | revert | 回滚到 266a466（保留 README/agent.md），恢复 UI 原始中文资源 | python -m unittest discover tests; python scripts/manual_regression_stage0.py; python scripts/smoke_timeline.py --tick 2000 --lead 3000 --early | - |
| 2025-11-11 19:09:24 UTC+01:00 | 5.1–5.3 | 阶段 5 功能验证：重跑 Stage0 用例、记录性能、清理遗留 | ✅ 已完成 | `python -m unittest discover tests; python scripts/manual_regression_stage0.py; python scripts/smoke_timeline.py --tick 2000 --lead 3000 --early`；UC-01~UC-07 全绿，仍仅有已知 `QThread` 警告；检视 `src/app` 无遗留 `_check_announcements_simple` 等废弃代码需清理。 |
| 2025-11-11 19:28:39 UTC+01:00 | A1–A3 | Main UI Stage A：拆分组件、集中绑定、复用 UiState | ✅ 已完成 | 新增 `app/ui/bindings.py`、补全组件返回值、`MainWindow` 通过回调注入快捷键/菜单/按钮；命令：`python -m unittest tests.test_timeline_service tests.test_file_repository`、`python scripts/manual_regression_stage0.py`（QThread 警告仍在）以及 `python scripts/smoke_timeline.py --tick 2000 --lead 3000 --early`。 |
| 2025-11-11 21:03:55 UTC+01:00 | B1-B3 | Main UI Stage B：引入 timeline/ocr controllers 与 actions、补齐集成测试 | ✅ 已完成 | 新增 `app/controllers/timeline_controller.py`、`app/controllers/ocr_controller.py`、`app/actions.py`；`MainWindow` 依赖注入 controller/action，`tests/test_timeline_controller.py` 与 `tests/test_ocr_controller.py` 覆盖关键链路。验证命令：`python -m unittest tests.test_timeline_service tests.test_file_repository tests.test_timeline_controller tests.test_ocr_controller tests.test_main_window`、`python -m unittest discover tests`（含 Qt warning）、`python scripts/manual_regression_stage0.py`（仍有已知 QThread 提示）、`python scripts/smoke_timeline.py --tick 2000 --lead 3000 --early`。 |
| 2025-11-11 22:45:00 UTC+08:00 | C1 | Main UI Stage C1：新增 `app/bootstrap.py`、HeadlessSpeechPort，并补 `tests/test_bootstrap.py` | python -m unittest discover -s tests -t . -v; python scripts/manual_regression_stage0.py; python scripts/smoke_timeline.py --tick 2000 --lead 3000 --early | `unittest` 全部通过，仅有 Qt 字体缺失 warning；`manual_regression_stage0.py` 在 UC05 后仍因真实 TTSWorker QThread 未停触发“QThread: Destroyed while thread '' is still running”并提前终止（将在 C2 通过 headless bootstrap 改造脚本）；`smoke_timeline` 正常完成 16 次播报。新增 bootstrap 入口/HeadlessSpeechPort 但尚未改动 README/脚本，待后续 C2 承接。 |
| 2025-11-12 07:03:54 UTC+08:00 | C2 | Main UI Stage C2：更新 `scripts/run_app.ps1`、README、计划文档与 `manual_regression_stage0.py`，统一使用 `python -m app.bootstrap` 并在 Stage0 中启用 HeadlessSpeechPort | python -m unittest discover -s tests -t . -v; python scripts/manual_regression_stage0.py; python scripts/smoke_timeline.py --tick 2000 --lead 3000 --early | README 新增 Run Book；计划/日志文档统一提示入口已切换至 `python -m app.bootstrap`；`manual_regression_stage0.py` 通过 bootstrap headless 注入，UC-05~07 可继续执行且输出 JSON。`unittest` 仍有 PyQt Qt6 字体 warning（历史已知）；Stage0 全绿；smoke 脚本 16 次播报正常。 |
